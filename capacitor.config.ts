import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'citasproyecto.danielreyes',
  appName: 'citasproyecto-danielreyes',
  webDir: 'www'
};

export default config;
